import styled from "styled-components";

export default styled.div`
  background-color: ${(props) => props.color};
  height: 2em;
  width: 2em;
  cursor: pointer;
  border: 0px;
  padding: 0;
  margin: 0;
`;
